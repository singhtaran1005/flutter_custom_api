import 'package:flutter/material.dart';
import 'package:flutter_custom_api/main/homepage.dart';

void main() {
  runApp(MyApp());
}
class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'a',
      home: MyHomePage(),
    );
  
  }
}